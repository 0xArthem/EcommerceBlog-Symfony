<div>
	<h4>
		{{ product.name }}
		<em>
			{% for category in product.category %}
				-
				{{ category.name }}
			{% endfor %}
		</em>

		<a href="{{ path('cart_add', {'id':product.id}) }}">
			Ajouter au panier
		</a>
	</h4>
</div>
